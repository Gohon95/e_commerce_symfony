{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
    <h1>Panier</h1>

    <table class="table">
        <tbody>
        {% for contenu in panier.contenuPaniers %}
            <tr>
                <th>{{'contenupanier.Id'|trans}}</th>
                <td>{{ panier.id }}</td>
            </tr>
            <tr>
                <th>{{'contenupanier.Montant'|trans}}</th>
                <td>{{ panier.totalAmount}}</td>
            </tr>
            <tr>
            <th>{{'contenupanier.Nom'|trans}}</th>
            <td> {{contenu.produit}}</td>
            </tr>
            <tr>
            <td>{{'contenupanier.Date_ajout'|trans}}</td>
                <td>{{panier.dateAchat | date() }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('panier_index') }}">back to list</a>

    <a href="{{ path('panier_edit', {'id': panier.id}) }}">edit</a>

    {{ include('panier/_delete_form.html.twig') }}
{% endblock %}
